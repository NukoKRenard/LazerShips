Fix ships crashing into each other

Fix attempts at increasing past max speed (probably due to ship colission code)

Impliment explosion effect

Replace skybox with a self made version.

Fix rolling issues